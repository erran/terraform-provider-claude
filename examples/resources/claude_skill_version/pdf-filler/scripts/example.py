"""Example helper for filling PDF form fields."""


def fill_form(input_path: str, output_path: str, fields: dict[str, str]) -> None:
    """Fill *fields* in the PDF at *input_path* and write to *output_path*."""
    raise NotImplementedError("Replace with a real PDF library such as pypdf.")
